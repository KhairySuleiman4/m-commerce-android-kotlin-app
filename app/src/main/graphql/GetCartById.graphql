query GetCartById($id: ID!){
    cart(id: $id) {
        id
        checkoutUrl
        lines(first: 10) {
            edges {
                node {
                    id
                    quantity
                    merchandise {
                        ... on ProductVariant {
                            id
                            title
                            price {
                                amount
                            }
                            product {
                                title
                                featuredImage {
                                    url
                                }
                                vendor
                            }
                        }
                    }
                }
            }
        }
        discountAllocations {
            discountedAmount {
                amount
            }
        }
        cost {
            subtotalAmount {
                amount
            }
            totalAmount {
                amount
            }
        }
    }
}